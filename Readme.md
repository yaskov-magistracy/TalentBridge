# Установка библиотек 

`pip install pypdf python-docx pandas faiss-cpu tiktoken langchain langchain-community`

`langchain-text-splitters langchain-huggingface`

`pip install sentence-transformers`

# Запуск приложения

`python GigaInstructions.py`

# Для HuggingFace

`hf auth login`

В папке /instructions находятся инструкции, взятые с Confluence 

В папке vector_db_instructions необходимые компоненты для работы с моделью HuggingFace