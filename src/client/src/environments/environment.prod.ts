/**
 * Environment Configuration - Production
 * 
 * Конфигурация окружения для продакшена.
 * В Docker контейнере значения могут быть переопределены
 * через переменные окружения во время сборки.
 */

export const environment = {
  /** Флаг режима продакшена */
  production: true,
  
  /** 
   * Базовый URL API
   * 
   * В продакшене обычно:
   * - Пустая строка для относительных путей (проксирование через nginx)
   * - Полный URL если фронтенд и бэкенд на разных доменах
   * - Значение из переменной окружения API_URL
   */
  apiUrl: '',  // Пустая строка = относительные пути (проксирование)
  
  /** Версия API */
  apiVersion: '/api/v1',
  
  /** Таймаут запросов в миллисекундах */
  requestTimeout: 30000,
  
  /** Настройки аутентификации */
  auth: {
    /** Ключ для хранения токена в localStorage */
    tokenKey: 'talentbridge_token',
    /** Ключ для хранения роли пользователя */
    roleKey: 'talentbridge_role',
    /** Ключ для хранения ID пользователя */
    userIdKey: 'talentbridge_user_id'
  }
};
